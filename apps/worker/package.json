{
  "name": "@mintro/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Playwright crawl worker. Runs on Fly.io — see docs/ARCHITECTURE.md.",
  "main": "./dist/src/index.js",
  "scripts": {
    "typecheck": "tsc --build"
  },
  "dependencies": {
    "@mintro/engine": "*",
    "@mintro/extraction": "*",
    "@mintro/ruleset": "*",
    "@supabase/supabase-js": "^2.47.0",
    "playwright": "^1.49.0"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.5"
  }
}
