{
  "name": "@mintro/ruleset",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Loader, schema and validator for rules/ruleset.json — the single source of truth for the RUO peptide program rule set.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./src/index.ts"
    }
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --build",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.23.8"
  }
}
